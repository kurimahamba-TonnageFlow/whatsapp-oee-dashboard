# Virtual Environment
.venv/

# Python Cache
__pycache__/
*.pyc

# VS Code Settings
.vscode/

# Operating System Files
.DS_Store
Thumbs.db
.env
.env.*
!.env.example

# PostgreSQL backups must never be committed
*.dump

# Node dependencies (anywhere in the tree)
node_modules/

# Claude Code local settings
.claude/

# WhatsApp Web Observer persistent browser profile (local only)
.pulse_browser_profile/

# WhatsApp Web Observer Phase 2 shadow-mode message inbox (local only)
.pulse_shadow_inbox/

# Stage 6B2A mocked visual-verification screenshots (local review only -
# generated from fictional data against a mocked API, never committed)
stage6b2_screenshots_tmp/
